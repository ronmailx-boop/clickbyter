# Cloudflare Worker (worker/) local development secrets.
# Copy relevant values into worker/.dev.vars for local `wrangler dev`,
# and set the real values in production with `wrangler secret put <NAME>`.
# Never commit real values.

GROQ_API_KEY=your_groq_api_key_here
