# Project State - Clickbyter

אפליקציית PWA שמפענחת כתבות "קליקבייט": משתפים/מדביקים קישור, האפליקציה קוראת את הכתבה במקום המשתמש ומציגה כותרת הכתבה + התשובה הקצרה שהכותרת הסתירה + קישור למקור.

תוכנית מלאה: `/root/.claude/plans/https-play-google-com-store-apps-details-dapper-quilt.md` (בסביבת התכנון; אינו חלק מה-repo).

## החלטות מפתח (סופיות)
- שם האפליקציה/ריפו: **Clickbyter** (`https://github.com/ronmailx-boop/clickbyter`).
- פלטפורמה: PWA, ללא build step - HTML/CSS/JS גולמיים ב-`public/`, פריסה ל-GitHub Pages.
- Backend: Cloudflare Worker (`worker/`, בשם `clickbyter-api`) - היחיד שיודע ליצור קריאות רשת ל-Groq ולכתבות חיצוניות (הגנת CORS + secrets).
- מנוע AI: **Groq** - מודל `openai/gpt-oss-20b` (לא `llama-3.1-8b-instant`, שהפך ל-Enterprise-only ומחזיר 404).
- הוספת קישורים: הדבקה ידנית (ברירת מחדל, תמיד זמינה, כולל חילוץ URL מטקסט חופשי/הודעות מועברות) + Web Share Target (Android בלבד, לאחר התקנה).
- פלט: כותרת הכתבה + תשובה קצרה + קישור למקור (הקישור המקורי **אחרי** מעקב הפניות - ר' "תיקונים אחרונים").
- היסטוריה: localStorage בלבד, בלי חשבונות, בלי סנכרון בין מכשירים.

## **האפליקציה חיה וכל 6 שלבי התוכנית המקורית הושלמו**
- כתובת האתר: `https://ronmailx-boop.github.io/clickbyter/`
- כתובת ה-Worker: `https://clickbyter-api.ronmailx.workers.dev`
- `GROQ_API_KEY` מוגדר כ-secret ב-Cloudflare, `CLOUDFLARE_API_TOKEN` כ-GitHub secret.
- **פענוח כתבה אמיתית (ynet) עבד מקצה לקצה בהצלחה** אחרי סבב תיקונים (ר' למטה).

## תיקונים אחרונים (אחרי המעבר לפרודקשן, כרונולוגי)
כל תיקון בוצע כ-commit נפרד על הענף, מוזג ל-main דרך PR, ונפרס אוטומטית ע"י GitHub Actions:
1. **Node 20→22** ב-`deploy-worker.yml` - Wrangler 4 דורש Node 22+.
2. **חילוץ URL מטקסט חופשי גם בהדבקה ידנית** (לא רק ב-share target) - תיקון מקרה של הדבקת הודעה מועברת ("מקור: X https://...").
3. **תיקון exception לא-מטופל בפרסור HTML** (`extract.ts`) - `parseHTML` רץ מחוץ ל-try/catch, קרס על HTML אמיתי → `UNKNOWN_ERROR` גנרי במקום `EXTRACTION_FAILED`.
4. **Service Worker לא קלט פריסות חדשות** - באג קריטי: `CACHE_NAME` לא התעדכן, אז cache-first הגיש קוד ישן לנצח. שונה ל-network-first (fallback ל-cache רק כשאין רשת) כדי שזה לא יקרה שוב.
5. **מודל Groq שגוי** - `llama-3.1-8b-instant` הפך ל-Enterprise-only (404). הוחלף ל-`openai/gpt-oss-20b`.
6. **הצגת כותרת הכתבה בכרטיס הראשי** (לא רק בהיסטוריה).
7. **פתרון קישורים מקוצרים/מפנים (bit.ly וכו')** - ה-fetch כבר עקב אחרי הפניות, אבל `sourceUrl` שהוחזר היה הקישור המקורי. עכשיו מוחזר `response.url` (הכתובת הסופית האמיתית).
8. שני מנגנוני אבחון זמניים נוספו והוסרו תוך כדי דיבוג (`detail` על שגיאות, `debugMethod`/`debugTextLength` על הצלחות - הוסר ב-revert לפי בקשה).
9. **(2026-09-14) תשובות נחתכות באמצע משפט** - המשתמש דיווח (צילום מסך מ-Newsly)
   על תשובת פענוח שנקטעת ("...ומבקשת" בלי המשך). אובחן: `openai/gpt-oss-20b`
   הוא מודל "reasoning" שמוציא חלק מ-`max_tokens` על חשיבה פנימית לפני
   כתיבת התשובה בפועל - `max_tokens: 200` הספיק לפעמים רק לחשיבה, והתשובה
   האמיתית נקטעה. **תוקן** ב-`worker/src/groq.ts`: נוסף `reasoning_effort:
   "low"` (המשימה כאן היא חילוץ פשוט, לא חשיבה עמוקה) והועלה `max_tokens`
   ל-600. **אומת בפרודקשן** (workflow אבחון זמני שהורץ ונמחק): שתי קריאות
   אמיתיות (walla, ynet) חזרו עם משפטים שלמים שמסתיימים בנקודה, ותוך
   3-4 שניות בלבד. **הערה לגבי תהליך:** התיקון הזה נדחף **ישירות ל-main**
   (לא commit על ענף + PR כמו בתיקונים הקודמים ברשימה הזו) - סטייה
   מהתהליך הרגיל, בעקבות דיווח דחוף מהמשתמש על תקלה חיה.

## נותר (לא חוסם שימוש בסיסי)
1. **בדיקה אמיתית על Android**: התקנה + שיתוף אמיתי מאפליקציה (יוטיוב/פייסבוק) - לא ניתן לבצע מתוך סביבת הפיתוח.
2. **החלטה על נתיב פריסה**: דומיין מותאם אישית מול `username.github.io/clickbyter/` (כרגע פועל עם הנתיב הרגיל).
3. **הגנת מכסה על ה-Worker**: אין כרגע rate limiting - סיכון תיאורטי שמישהו שימצא את כתובת ה-Worker ינצל את מכסת Groq החינמית.

## הושלם (אחרי הפריסה)
- **מסמכים משפטיים**: שדות ה-`[PLACEHOLDER]` ב-`docs/legal/*.md` מולאו (תאריך, ronmailx@gmail.com כאיש קשר, הצהרה כנה שלא בוצעה בדיקת נגישות חיצונית מוסמכת).
- **קישור bit.ly אמיתי** - נבדק ע"י המשתמש בפועל ועובד (התיקון ל-`sourceUrl`/`finalUrl` אושר).

### הערה: מגבלת רשת בסביבת הפיתוח (session זה)
גישת רשת יוצאת בסביבת הפיתוח חסומה לרוב האינטרנט (רק registry.npmjs.org, GitHub וכו') - כל הבדיקות נגד Groq אמיתי/אתרי חדשות אמיתיים/Cloudflare האמיתי נעשות רק ע"י המשתמש בפועל, לא מתוך הסשן. שינויים בקוד נבדקים ביחידה (unit tests עם mocking) לפני כל push.

## החלטה שהתבטלה
תוכנית קודמת לאפליקציית ארגון קישורי סרטונים (YouTube/Facebook עם Supabase+Google Auth) בוטלה לגמרי לפי בקשת המשתמש - לא רלוונטית יותר.
