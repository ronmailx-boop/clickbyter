name = "clickbyter-api"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# The exact origin allowed to call this API (CORS). Update to the real
# deployed GitHub Pages URL, e.g. "https://<user>.github.io" or a custom domain.
ALLOWED_ORIGIN = "http://localhost:8791"

# Secrets (never set here - use `wrangler secret put GROQ_API_KEY` instead):
#   GROQ_API_KEY
